\documentclass{article}

% NeurIPS 2026 workshop, double-blind reviewing.
\usepackage[dblblindworkshop]{neurips_2026}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{threeparttable}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage[expansion=false]{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,fit,positioning}
\hypersetup{hidelinks,hypertexnames=false}
\newcommand{\runin}[1]{\par\medskip\noindent\textbf{#1}\ }

% Shared diagram language: blue denotes attention, orange denotes state-space
% computation, and gray denotes inputs, outputs, or evaluation bookkeeping.
\definecolor{attentionblue}{HTML}{2C7FB8}
\definecolor{ssmorange}{HTML}{E67E22}
\definecolor{diagramgray}{HTML}{5B6573}
\definecolor{lightblue}{HTML}{EAF3F8}
\definecolor{lightorange}{HTML}{FDF0E5}
\tikzset{
  diagram node/.style={draw=diagramgray!75, rounded corners=1.5pt,
    fill=white, minimum height=0.42cm, inner xsep=3pt, inner ysep=2pt,
    font=\scriptsize\sffamily},
  attention node/.style={diagram node, draw=attentionblue, fill=lightblue},
  ssm node/.style={diagram node, draw=ssmorange, fill=lightorange},
  diagram arrow/.style={-{Latex[length=1.4mm]}, line width=0.45pt,
    draw=diagramgray!85},
  attention arrow/.style={diagram arrow, draw=attentionblue},
  ssm arrow/.style={diagram arrow, draw=ssmorange}
}

\title{Mixing Matters: Evidence Position Bias Across Sequence Mixers in
Long-Context Question Answering}

\workshoptitle{New in ML}

\author{Anonymous Author(s)}

\begin{document}

\maketitle

\begin{abstract}
Language models often use relevant evidence less effectively when it appears in the middle of a long context.
We ask whether this position bias changes with the mechanism used to mix information across tokens.
For each multi-document question, we move the answer-bearing document through ten positions while holding the question and distractors fixed.
In a shared-corpus comparison near 2.8B parameters, Pythia shows a clear beginning advantage that two Mamba variants lack, while all three models show an end advantage.
This near-2.8B family comparison confounds sequence mixing with depth, RoPE or other positional encoding, recurrent-state size, and checkpoint family.
A tighter comparison between pure and hybrid Mamba-2 models points in the same direction, but its paired effect remains statistically uncertain.
Across additional exploratory studies, the family gap appears only once the compared models can answer the task, a corpus swap changes overall accuracy without a detectable change in curve shape, and Pythia reproduces the beginning advantage on synthetic retrieval.
Prompt placement can materially reshape both edges.
Attention-sink measurements and position probes motivate hypotheses about evidence use, but do not establish a causal mechanism.
All ten-document QA results use an exploratory split, and the held-out confirmatory split remains unexamined.
\end{abstract}

\section{Introduction}
\label{sec:intro}

Retrieval-augmented systems often present a model with several documents and expect it to use whichever one contains the answer.
Yet evidence is not equally usable everywhere in the prompt: accuracy commonly falls when the relevant document moves from an edge toward the middle \citep{liu2024lost}.
This failure is important even when retrieval itself succeeds, because a downstream model can still neglect the retrieved evidence.
Long-context benchmarks expose related weaknesses across question answering, summarization, and retrieval \citep{bai2024longbench,shaham2023zeroscrolls,li2024loogle}.

The usual lost-in-the-middle curve has mostly been studied in Transformers.
Dense attention can revisit any earlier token directly, whereas state-space models such as Mamba compress the prefix into a recurrent state \citep{gu2023mamba,dao2024mamba2}.
This architectural difference could change how evidence position affects an answer.
It is difficult to test cleanly, however, because model families also differ in training data, tokenizer, depth, scale, positional encoding, and capability.

We study the question with a paired intervention on evidence position.
For every question, the same answer document and distractors form one bundle, and only the answer document's position changes.
The main comparison finds a beginning advantage for Pythia that is absent in two similarly sized Mamba models trained on the same corpus, while the end advantage is shared.
This near-2.8B family comparison confounds sequence mixing with depth, RoPE or other positional encoding, recurrent-state size, and checkpoint family.
A more tightly matched pure-versus-hybrid Mamba-2 comparison is directionally compatible but does not detect a reliable paired attention effect.
Scale, corpus, task, production-model, prompt, sink, and probe studies then define where the observation does and does not generalize.

The paper makes three contributions.
First, it provides an auditable position-intervention harness with anchors, preserved outputs, paired inference, and an untouched confirmatory split.
Second, it presents an exploratory family-by-position comparison together with a tighter but statistically uncertain attention comparison.
Third, it turns the remaining phases into bounded evidence: corpus and task checks test scope, while prompt interventions and internal measurements generate mechanistic hypotheses without being presented as causal proof.

\section{Related work}
\label{sec:related}

\runin{Position bias in long contexts}
\citet{liu2024lost} established the lost-in-the-middle curve in retrieval and multi-document QA.
This task derives its questions from Natural Questions \citep{kwiatkowski2019nq}, while LongBench, ZeroSCROLLS, and LooGLE broaden long-context evaluation across tasks, languages, and reasoning demands \citep{bai2024longbench,shaham2023zeroscrolls,li2024loogle}.
Subsequent work measures effective context length \citep{hsieh2024ruler}, associates the curve with attention allocation \citep{hsieh2024found}, and proposes reordering, compression, retrieval, and context-use interventions \citep{an2024make,peysakhovich2023attention,jiang2024longllmlingua,xu2024recomp,agrawal2024rr}.
Irrelevant context can itself distract a model, motivating our use of the same distractors at every tested position \citep{shi2023distracted}.
We focus on whether the curve changes across sequence mixers.

\runin{Sequence mixers}
Dense self-attention \citep{vaswani2017attention} mixes token pairs under a causal mask and commonly encodes order with rotary embeddings or linear attention biases \citep{su2024roformer,press2022alibi}.
Alternative long-sequence mixers include structured state spaces, recurrent weighted key-value models, and long convolutions \citep{gu2022s4,peng2023rwkv,poli2023hyena}.
Mamba and Mamba-2 carry information through selective state-space recurrences \citep{gu2023mamba,dao2024mamba2}.
Hybrid models such as Griffin, Jamba, and Nemotron-H combine recurrent or state-space blocks with attention \citep{de2024griffin,lieber2024jamba,nvidia2025nemotronh}.
\citet{waleffe2024empirical} release the pure and hybrid 8B Mamba-2 checkpoints used in our tightest controlled comparison.

% preamble: \usetikzlibrary{positioning, fit, arrows.meta, calc}

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  node distance=3mm and 3mm,
  every node/.style={font=\small\sffamily},
]

% ================= Panel 1: Dense attention =================
\node[diagram node] (a1) {$x_1$};
\node[diagram node, right=of a1] (a2) {$x_2$};
\node[diagram node, right=of a2] (a3) {$\cdots$};
\node[diagram node, right=of a3] (a4) {$x_T$};
\node[attention node] (ao) at ($(a2)!0.5!(a3)+(0,-11mm)$) {$h_t$};
\foreach \n in {a1,a2,a3,a4} {\draw[attention arrow] (\n) -- (ao);}

\node[fit=(a1)(a4), inner sep=0pt] (Prow1) {};
\node[font=\scriptsize\sffamily, text=attentionblue, text width=3.9cm,
      align=center, above=1.2mm of Prow1] (Pann1) {direct token-to-state mixing};
\node[font=\small\bfseries, above=0.8mm of Pann1] {Dense attention};

% ================= Panel 2: State-space recurrence =================
\node[diagram node, right=17mm of a4] (s1) {$x_1$};
\node[diagram node, right=of s1] (s2) {$x_2$};
\node[diagram node, right=of s2] (s3) {$\cdots$};
\node[diagram node, right=of s3] (s4) {$x_T$};

\node[ssm node, below=9mm of s1] (z1) {$z_1$};
\node[ssm node, below=9mm of s2] (z2) {$z_2$};
\node[ssm node, below=9mm of s3] (z3) {$z_{t-1}$};
\node[ssm node, below=9mm of s4] (z4) {$z_t$};
\foreach \x/\z in {s1/z1,s2/z2,s3/z3,s4/z4} {\draw[ssm arrow] (\x) -- (\z);}
\foreach \u/\v in {z1/z2,z2/z3,z3/z4} {\draw[ssm arrow] (\u) -- (\v);}

\node[fit=(s1)(s4), inner sep=0pt] (Prow2) {};
\node[font=\scriptsize\sffamily, text=ssmorange, text width=4.3cm,
      align=center, above=1.2mm of Prow2] (Pann2) {recurrent state carries the prefix};
\node[font=\small\bfseries, above=0.8mm of Pann2] {State-space recurrence};

% ================= Panel 3: Hybrid mixer =================
\node[diagram node, right=17mm of s4] (h1) {$x_1$};
\node[diagram node, right=of h1] (h2) {$x_2$};
\node[diagram node, right=of h2] (h3) {$\cdots$};
\node[diagram node, right=of h3] (h4) {$x_T$};

\node[ssm node, below=9mm of h1] (r1) {$z_1$};
\node[ssm node, below=9mm of h2] (r2) {$z_2$};
\node[attention node, below=9mm of h3, minimum width=1.05cm] (ha) {attn.};
\node[ssm node, below=9mm of h4] (r4) {$z_t$};

\foreach \x/\z in {h1/r1,h2/r2,h4/r4} {\draw[ssm arrow] (\x) -- (\z);}
\draw[ssm arrow] (r1) -- (r2);
\draw[ssm arrow] (r2) -- (ha);
\draw[attention arrow] (h3) -- (ha);
\draw[attention arrow] (ha) -- (r4);

\node[fit=(h1)(h4), inner sep=0pt] (Prow3) {};
\node[font=\scriptsize\sffamily, text=attentionblue, text width=4.3cm,
      align=center, above=1.2mm of Prow3] (Pann3) {recurrent path with attention blocks};
\node[font=\small\bfseries, above=0.8mm of Pann3] {Hybrid mixer};

\end{tikzpicture}
\caption{Schematic abstractions of the sequence-mixing mechanisms discussed in this paper. Dense attention permits direct token-to-state interactions, state-space models carry prefix information through a recurrent state, and hybrid models combine recurrent blocks with a minority of attention blocks. The drawing is conceptual rather than a layer-by-layer specification, and the depicted 1-in-4 attention block is illustrative only: the hybrid checkpoint evaluated in Section~\ref{sec:contrast} replaces about 7\% of blocks with attention, a substantially smaller fraction than shown here.}
\label{fig:mixer-schematic}
\end{figure*}

\runin{Cross-architecture long-context evidence}
Architecture comparisons do not imply that recurrent models are uniformly position-robust.
\citet{huang2025well} find practical long-context failures across recurrent, hybrid, and Transformer designs, and a concurrent preprint reports a U-shaped Mamba recall curve on structured memory tasks \citep{airlangga2025primacy}.
These results differ from our Mamba QA curve and reinforce a central boundary of this study: position profiles depend on the task and prompt, not only the mixer.
Work that reformulates Mamba recurrence as implicit attention offers a complementary route for future mechanism tests \citep{ali2025hidden}.

\runin{Attention sinks and probes}
\citet{xiao2024efficient} showed that causal transformers can concentrate attention on initial tokens, and \citet{sun2024massive} connected unusually large internal activations to concentrated attention.
We test whether this attention-sink statistic covaries with primacy, treating the result as correlational because a valid sink-blocking ablation is absent.
Our position probe follows the principle that a diagnostic classifier needs a control task before its accuracy can be interpreted as evidence about a representation \citep{hewitt2019probes}.

\section{Measurement and statistics}
\label{sec:method}

\runin{Task and anchors}
We use the 2{,}655 released questions from the Lost-in-the-Middle 10-document QA setting \citep{liu2024lost}, which is built from Natural Questions \citep{kwiatkowski2019nq}.
Each question has one gold document and nine distractors.
We build ten prompts per question, moving only the gold document through positions $0$ to $9$ while retaining the same distractors and token budget.
A no-gold floor measures guessing or memorized knowledge, and a gold-only ceiling measures answerability under perfect retrieval.
The fixed split uses seed 240521 and contains 800 exploratory and 1{,}855 confirmatory questions.
Every reported ten-document QA result uses the exploratory allocation or a subset of it.
The confirmatory allocation remains unopened.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  diagram node/.append style={font=\small\sffamily, inner sep=5pt, minimum height=6mm},
  ssm node/.append style={font=\small\sffamily, inner sep=5pt, minimum height=6mm},
]
  \node[diagram node]             (q) {question};
  \node[diagram node, right=of q] (d) {9 distractors};
  \node[ssm node,     right=of d] (g) {gold doc.};
  \node[diagram node, right=of g] (p) {prompt};

  \draw[diagram arrow] (q) -- (d);
  \draw[diagram arrow] (d) -- (g);
  \draw[diagram arrow] (g) -- (p);

  \node[draw=diagramgray, dashed, rounded corners,
        inner xsep=7pt, inner ysep=6pt, fit=(q)(d)(g),
        label={[font=\scriptsize\sffamily, text=diagramgray]above:same bundle}] {};

  \node[font=\scriptsize\sffamily, align=center, below=14mm of d]
    {repeat for gold position $0,\dots,9$\\[1pt]
     question, distractors, and budget fixed};

  \node[diagram node, below left=18mm and 6mm of p]  (floor) {no-gold floor};
  \node[diagram node, below right=18mm and 6mm of p] (ceil)  {gold-only ceiling};

  \draw[diagram arrow] (floor) -- (p.south);
  \draw[diagram arrow] (ceil)  -- (p.south);
\end{tikzpicture}
\caption{Paired position intervention. Each question contributes one fixed bundle and ten prompts; only the gold document's position changes. The no-gold and gold-only anchors bound guessing and answerability.}
\label{fig:position-harness}
\end{figure}

\runin{Edges and statistics}
Let $a_S$, $a_M$, and $a_E$ be mean accuracy at start positions $\{0,1\}$, middle positions $\{4,5\}$, and end positions $\{8,9\}$.
We define primacy as $a_S-a_M$ and recency as $a_E-a_M$.
The question is the unit of analysis.
Each of 10{,}000 bootstrap samples draws complete ten-position question bundles with replacement, following the bootstrap principle of resampling the independent unit \citep{efron1979bootstrap}, and model comparisons reuse the same sampled questions.
We report percentile 95\% confidence intervals and apply Holm's sequential correction to the primacy and recency tests within each comparison family \citep{holm1979multiple}.
The intervals quantify variation across questions under the fixed checkpoints, prompts, and decoding configuration, not variation across training seeds or model checkpoints.

\runin{Reproducibility and controls}
The harness vendors the official prompt builder and \texttt{best\_subspan\_em} scorer at a pinned upstream commit.
It also records normalized exact match, following the normalization convention popularized by SQuAD \citep{rajpurkar2016squad}, and first-line extraction.
Generation is greedy with temperature $0$, at most 32 new tokens, fixed seeds, BF16 where supported, pinned model revisions, and a fixed Mamba execution path within each controlled contrast.
Each phase fixes one execution path within its controlled comparison, although the Transformers and Megatron backends differ across phases.
Committed standard QA sweeps preserve prompts, raw generations, failures, token counts, software versions, and decoding settings.
The synthetic retrieval check is represented by summaries and plots only; raw generations and an environment manifest are not available in the repository snapshot.
The released key-value task provides an executed positive control: the pipeline detects a $+0.38$ Pythia edge-versus-middle effect.
For the sham-gold control, accuracy was $0.1235$ versus a $0.0950$ floor, a difference of $0.0285$; primacy was $+0.0050$ with 95\% CI $[-0.0325, 0.0425]$, and recency was $-0.0175$ with 95\% CI $[-0.0500, 0.0125]$.
Distractor-order permutation accuracies were $0.2850$, $0.3000$, and $0.2767$, with a maximum spread of $0.0233$.
Both certification controls passed their gates on the 200-question exploratory Pythia sample only, and they do not test the 8B Megatron checkpoints.

\section{Architecture comparisons}
\label{sec:contrast}

\runin{Primary 2.8B comparison}
We compare \texttt{pythia-2.8b} \citep{biderman2023pythia} with \texttt{mamba-2.8b} \citep{gu2023mamba} and \texttt{mamba2-2.7b} \citep{dao2024mamba2}.
All are base models pretrained on the Pile \citep{gao2020pile}, have similar parameter counts, and use related tokenizers.
Table~\ref{tab:phase2} and Figure~\ref{fig:primary-curves} show a positive primacy edge for Pythia and no positive primacy edge for either Mamba model.
The paired Mamba-minus-Pythia primacy interactions are $-0.053$ and $-0.070$ (both Holm $p<10^{-4}$), while Mamba-1 versus Mamba-2 is uncertain ($+0.017$, 95\% CI $[-0.005,0.038]$).
All three models have positive recency edges.
These comparisons match corpus and approximate scale, but not depth, positional encoding, state size, or checkpoint family.

\begin{table*}[t]
\centering
\caption{Pile-trained models on 800 exploratory questions. Paired-bootstrap 95\% CIs and
Holm-corrected $p$-values across six tests; bold $p$ indicates $\alpha=0.05$ significance.}
\label{tab:phase2}
\scriptsize
\begin{threeparttable}
\setlength{\tabcolsep}{2.5pt}
\renewcommand{\arraystretch}{1.0}
\begin{tabular}{@{}l
                S[table-format=+1.3]
                c
                c
                S[table-format=+1.3]
                c
                c
                S[table-format=1.3]
                S[table-format=1.3]
                @{}}
\toprule
& \multicolumn{3}{c}{Primacy} & \multicolumn{3}{c}{Recency} & \multicolumn{2}{c}{Anchors} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7} \cmidrule(lr){8-9}
Model & {$\Delta$} & {95\% CI} & {$p$} & {$\Delta$} & {95\% CI} & {$p$} & {Floor} & {Ceiling} \\
\midrule
\texttt{pythia-2.8b}  & 0.052  & $[0.032, 0.072]$   & {\bfseries\boldmath${<}10^{-4}$} & 0.075 & $[0.054, 0.097]$ & {\bfseries\boldmath${<}10^{-4}$} & 0.091 & 0.640 \\
\texttt{mamba-2.8b}   & -0.001 & $[-0.016, 0.014]$  & $0.91$                           & 0.077 & $[0.058, 0.097]$ & {\bfseries\boldmath${<}10^{-4}$} & 0.115 & 0.615 \\
\texttt{mamba2-2.7b}  & -0.018 & $[-0.034, -0.003]$ & {\bfseries\boldmath$0.02$}        & 0.105 & $[0.086, 0.124]$ & {\bfseries\boldmath${<}10^{-4}$} & 0.128 & 0.619 \\
\bottomrule
\end{tabular}
\begin{tablenotes}
\footnotesize
\item[] $\Delta$ is anchor-minus-middle accuracy; Floor/Ceiling are the no-gold/gold-only anchors from Fig.~\ref{fig:position-harness}.
\end{tablenotes}
\end{threeparttable}
\end{table*}

\begin{figure}[t]
\centering
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/paper-phase2-position.pdf}
\textbf{(a) Pile-trained models near 2.8B parameters}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/paper-phase3-position.pdf}
\textbf{(b) Matched pure and hybrid Mamba-2 models at 8B}
\end{minipage}
\caption{Accuracy as the answer-bearing document moves through the context on 800 exploratory questions.
Pythia rises at the beginning, while both smaller Mamba variants do not; the matched 8B hybrid also rises more than the pure model, but their paired difference remains uncertain.
Shaded bands show 95\% question-bootstrap confidence intervals.}
\label{fig:primary-curves}
\end{figure}

\runin{Matched 8B attention comparison}
Our strongest control uses NVIDIA's pure and hybrid 8B Mamba-2 checkpoints \citep{waleffe2024empirical}, which share training data, tokenizer, scale, depth, and positional setup.
The hybrid contains 7\% attention layers.
Within models, the pure model's primacy edge is uncertain ($+0.008$, $p=0.36$) and the hybrid's is positive ($+0.027$, $p=0.007$).
One estimate being significant and the other not does not establish that the models differ.
The paired hybrid-minus-pure primacy effect is $+0.019$ (95\% CI $[-0.006,0.044]$, Holm $p=0.144$), while the recency effect is $-0.028$ (95\% CI $[-0.055,-0.001]$, Holm $p=0.089$).
This direction supports the 2.8B observation but does not isolate a causal attention effect.

\begin{table}[t]
\caption{Matched 8B primacy comparison on 800 exploratory questions.
The paired interaction is the relevant architecture test.}
\label{tab:phase3}
\centering
\small
\begin{tabular}{lccc}
\toprule
Contrast & Primacy & 95\% CI & Holm $p$ \\
\midrule
pure Mamba-2 & $+0.008$ & $[-0.009,0.024]$ & $0.362$ \\
hybrid & $+0.027$ & $[0.008,0.047]$ & $0.007$ \\
hybrid minus pure & $+0.019$ & $[-0.006,0.044]$ & $0.144$ \\
\bottomrule
\end{tabular}
\end{table}

\section{Scope checks}
\label{sec:robust}

\runin{Scale}
Across five approximate size pairs, the Pythia-minus-Mamba primacy gap is near zero at the two smallest scales and is $+0.062$, $+0.069$, and $+0.053$ in the three larger pairs (Figure~\ref{fig:artifact-scale-corpus}).
The smallest Pythia ceiling is only $0.009$, so this is a descriptive capability-and-family trend, not evidence for a depth threshold or a causal scale transition.

\runin{Training corpus}
Holding the Mamba-2.8B architecture fixed, the Pile-minus-SlimPajama primacy effect is $-0.007$ (95\% CI $[-0.030,0.016]$, $p=0.568$) and the recency effect is $-0.016$ (95\% CI $[-0.044,0.012]$, Holm $p=0.502$) \citep{soboleva2023slimpajama}.
The floor differs substantially ($0.270$ versus $0.114$), so this comparison finds no edge-shape change but does not rule out training-data effects (Figure~\ref{fig:artifact-scale-corpus}).

\runin{Synthetic retrieval}
On 50 RULER \texttt{niah\_single\_1} examples at 2K tokens \citep{hsieh2024ruler}, Pythia again has a primacy edge ($+0.12$, 95\% CI $[0.05,0.20]$).
Both Mamba models score $1.0$ at every depth, so this check reproduces the transformer pattern but cannot compare mixer curves on the synthetic task (Figure~\ref{fig:artifact-task}).

\runin{Descriptive system comparison}
We also run \texttt{Llama-3.1-8B} \citep{dubey2024llama}, \texttt{Qwen2.5-7B} \citep{yang2024qwen2}, and \texttt{Nemotron-H-8B} \citep{nvidia2025nemotronh}.
All three show positive primacy edges ($+0.076$, $+0.041$, $+0.067$).
Only Llama versus Qwen has a significant paired primacy interaction ($+0.036$, Holm $p=0.016$).
Because the systems differ on architecture, data, tokenizer, token count, depth, and positional encoding, these results establish prevalence, not an architectural explanation (Figure~\ref{fig:artifact-production}).

\section{Mechanistic evidence and intervention}
\label{sec:mechanism}

\runin{Attention sinks track primacy}
Using the initial-token attention-share statistic of \citet{xiao2024efficient}, we measure almost no final-layer token-0 sink in Pythia-160M ($0.003$), while Pythia-2.8B reaches $0.455$.
Pythia-410M has a small final-layer sink ($0.034$) and no significant primacy edge.
Figure~\ref{fig:artifact-mechanisms} shows a scale-linked association, but the measurements share model scale as a confound and no valid sink ablation was completed.

\runin{Utilization, not storage, is a hypothesis}
A balanced linear probe on frozen mid-depth states predicts edge versus middle position modestly above its shuffled-label control for Pythia ($0.603$ versus $0.484$, a 12-point lift) and Mamba ($0.648$ versus $0.508$, a 14-point lift).
Thus position information remains linearly decodable, at a modest margin over the shuffled baseline, even when Mamba has no positive primacy edge.
This is consistent with a utilization hypothesis, but the effect size is modest and probe decodability does not prove that the model stores or uses the answer-bearing content.

\runin{Training-free prompt intervention}
On 200 exploratory questions, repeating the question both before and after the documents changes Mamba primacy from $0.000$ to $+0.103$ ($p<10^{-4}$).
Placing the question only before the documents does not create primacy ($+0.020$, $p=0.366$), but reduces recency from $+0.070$ to $+0.010$.
The bookend result is not a mitigation result: much of the larger edge comes from lower middle-position accuracy.
It also changes query repetition and token placement together, so it is not a clean test of fixed-state compression.

\runin{Prompt sensitivity}
Changing the prompt template reduces Pythia primacy from $+0.070$ to $+0.005$ in the 200-question mechanism subset.
This result warns against treating the measured magnitude as a template-invariant architectural constant.

\section{Discussion and limitations}
\label{sec:discussion}

The most secure result is the 2.8B exploratory family-by-position interaction.
The matched 8B direction, sink association, and probe results motivate a mixer-dependent evidence-use hypothesis, while prompt sensitivity shows that architecture is not the only determinant.

Several limitations set the confirmatory boundary.
All analyses use the exploratory split, so the planned 1{,}855-question test must be run under a frozen analysis before confirmatory language is warranted.
The 2.8B and scale comparisons retain depth, positional-encoding, state-size, and capability confounds.
The matched 8B paired contrast crosses zero after Holm correction; an independent clean re-run at commit \texttt{33d6bb5} (clean source tree, both repositories) reproduced the summary byte-for-byte, so the null result is not an artifact of the earlier dirty-tree run.
These certification controls passed on Pythia, but they do not test the 8B Megatron checkpoints.
A 50-generation blinded audit sample exists, but human labels were not completed.
Finally, the attempted hybrid sink block did not alter the model because its custom attention path did not honor the hook; it is not an ablation result.
The next decisive study should freeze exclusions and contrasts, repair the hybrid intervention, and evaluate the held-out split once.

\runin{Broader impact}
Position-sensitive evidence use can make retrieval-augmented systems unreliable even when retrieval succeeds.
An evaluation that exposes this failure can support better ordering strategies and more honest model selection.
The same phenomenon can also cause systems to privilege facts because of placement rather than relevance, so reported long-context accuracy should not be treated as uniform evidence access.
This work releases diagnostics and outputs, not a new generative model.

\section{Conclusion}
\label{sec:conclusion}

On the exploratory Lost-in-the-Middle split, Pythia-2.8B and two Pile-trained Mamba models have different primacy profiles under a paired ten-position evaluation.
A matched 8B comparison is directionally consistent but statistically uncertain, so the present evidence supports a family-by-position interaction without establishing that attention causes it.
The held-out split and extension of the certification controls beyond Pythia provide a clear path from this exploratory workshop result to a confirmatory study.

{
\small
\begin{thebibliography}{99}

\bibitem[Agrawal et al.(2024)]{agrawal2024rr}
D.~Agrawal, S.~Gao, and M.~Gajek.
\newblock Can't remember details in long documents? You need some R\&R.
\newblock In \emph{Findings of the Association for Computational Linguistics: EMNLP}, pages 12692-12704, 2024.

\bibitem[Airlangga et al.(2025)]{airlangga2025primacy}
M.~C. Airlangga, H.~AlQuabeh, M.~S. Nwadike, and K.~Inui.
\newblock Emergence of primacy and recency effect in Mamba: A mechanistic point of view.
\newblock \emph{arXiv:2506.15156}, 2025.

\bibitem[Ali et al.(2025)]{ali2025hidden}
A.~A. Ali, I.~Zimerman, and L.~Wolf.
\newblock The hidden attention of Mamba models.
\newblock In \emph{Proceedings of the 63rd Annual Meeting of the Association for Computational Linguistics}, pages 1516-1534, 2025.

\bibitem[An et al.(2024)]{an2024make}
S.~An, Z.~Ma, Z.~Lin, N.~Zheng, J.-G. Lou, and W.~Chen.
\newblock Make your LLM fully utilize the context.
\newblock In \emph{Advances in Neural Information Processing Systems}, volume 37, 2024.

\bibitem[Bai et al.(2024)]{bai2024longbench}
Y.~Bai, X.~Lv, J.~Zhang, et al.
\newblock LongBench: A bilingual, multitask benchmark for long context understanding.
\newblock In \emph{Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics}, pages 3119-3137, 2024.

\bibitem[Biderman et al.(2023)]{biderman2023pythia}
S.~Biderman, H.~Schoelkopf, Q.~Anthony, et al.
\newblock Pythia: A suite for analyzing large language models across training and scaling.
\newblock In \emph{International Conference on Machine Learning}, volume 202 of PMLR, pages 2397-2430, 2023.

\bibitem[Dao and Gu(2024)]{dao2024mamba2}
T.~Dao and A.~Gu.
\newblock Transformers are SSMs: Generalized models and efficient algorithms through structured state space duality.
\newblock In \emph{International Conference on Machine Learning}, volume 235 of PMLR, pages 10041-10071, 2024.

\bibitem[De et al.(2024)]{de2024griffin}
S.~De, S.~L. Smith, A.~Fernando, et al.
\newblock Griffin: Mixing gated linear recurrences with local attention for efficient language models.
\newblock \emph{arXiv:2402.19427}, 2024.

\bibitem[Dubey et al.(2024)]{dubey2024llama}
A.~Dubey, A.~Jauhri, A.~Pandey, et al.
\newblock The Llama 3 herd of models.
\newblock \emph{arXiv:2407.21783}, 2024.

\bibitem[Efron(1979)]{efron1979bootstrap}
B.~Efron.
\newblock Bootstrap methods: Another look at the jackknife.
\newblock \emph{The Annals of Statistics}, 7(1):1-26, 1979.

\bibitem[Gao et al.(2020)]{gao2020pile}
L.~Gao, S.~Biderman, S.~Black, et al.
\newblock The Pile: An 800GB dataset of diverse text for language modeling.
\newblock \emph{arXiv:2101.00027}, 2020.

\bibitem[Gu and Dao(2024)]{gu2023mamba}
A.~Gu and T.~Dao.
\newblock Mamba: Linear-time sequence modeling with selective state spaces.
\newblock In \emph{Conference on Language Modeling}, 2024.

\bibitem[Gu et al.(2022)]{gu2022s4}
A.~Gu, K.~Goel, and C.~R\'e.
\newblock Efficiently modeling long sequences with structured state spaces.
\newblock In \emph{International Conference on Learning Representations}, 2022.

\bibitem[Hewitt and Liang(2019)]{hewitt2019probes}
J.~Hewitt and P.~Liang.
\newblock Designing and interpreting probes with control tasks.
\newblock In \emph{Proceedings of EMNLP-IJCNLP}, pages 2733-2743, 2019.

\bibitem[Holm(1979)]{holm1979multiple}
S.~Holm.
\newblock A simple sequentially rejective multiple test procedure.
\newblock \emph{Scandinavian Journal of Statistics}, 6(2):65-70, 1979.

\bibitem[Huang(2025)]{huang2025well}
J.~Huang.
\newblock How well can a long sequence model model long sequences? Comparing architectural inductive biases on long-context abilities.
\newblock In \emph{Proceedings of the 31st International Conference on Computational Linguistics}, pages 29-39, 2025.

\bibitem[Hsieh et al.(2024a)]{hsieh2024found}
C.-Y. Hsieh, Y.-S. Chuang, C.-L. Li, Z.~Wang, L.~Le, A.~Kumar, J.~Glass, A.~Ratner, C.-Y. Lee, R.~Krishna, and T.~Pfister.
\newblock Found in the middle: Calibrating positional attention bias improves long context utilization.
\newblock In \emph{Findings of the Association for Computational Linguistics: ACL}, pages 14982-14995, 2024.

\bibitem[Hsieh et al.(2024b)]{hsieh2024ruler}
C.-P. Hsieh, S.~Sun, S.~Kriman, et al.
\newblock RULER: What's the real context size of your long-context language models?
\newblock In \emph{Conference on Language Modeling}, 2024.

\bibitem[Jiang et al.(2024)]{jiang2024longllmlingua}
H.~Jiang, Q.~Wu, X.~Luo, et al.
\newblock LongLLMLingua: Accelerating and enhancing LLMs in long context scenarios via prompt compression.
\newblock In \emph{Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics}, pages 1658-1677, 2024.

\bibitem[Kwiatkowski et al.(2019)]{kwiatkowski2019nq}
T.~Kwiatkowski, J.~Palomaki, O.~Redfield, et al.
\newblock Natural Questions: A benchmark for question answering research.
\newblock \emph{Transactions of the Association for Computational Linguistics}, 7:452-466, 2019.

\bibitem[Lieber et al.(2024)]{lieber2024jamba}
O.~Lieber, B.~Lenz, H.~Bata, et al.
\newblock Jamba: A hybrid Transformer-Mamba language model.
\newblock \emph{arXiv:2403.19887}, 2024.

\bibitem[Li et al.(2024)]{li2024loogle}
J.~Li, M.~Wang, Z.~Zheng, and M.~Zhang.
\newblock LooGLE: Can long-context language models understand long contexts?
\newblock In \emph{Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics}, pages 16304-16333, 2024.

\bibitem[Liu et al.(2024)]{liu2024lost}
N.~F. Liu, K.~Lin, J.~Hewitt, A.~Paranjape, M.~Bevilacqua, F.~Petroni, and P.~Liang.
\newblock Lost in the middle: How language models use long contexts.
\newblock \emph{Transactions of the Association for Computational Linguistics}, 12:157-173, 2024.

\bibitem[NVIDIA(2025)]{nvidia2025nemotronh}
NVIDIA.
\newblock Nemotron-H: A family of accurate and efficient hybrid Mamba-Transformer models.
\newblock \emph{arXiv:2504.03624}, 2025.

\bibitem[Peysakhovich and Lerer(2023)]{peysakhovich2023attention}
A.~Peysakhovich and A.~Lerer.
\newblock Attention sorting combats recency bias in long context language models.
\newblock \emph{arXiv:2310.01427}, 2023.

\bibitem[Peng et al.(2023)]{peng2023rwkv}
B.~Peng, E.~Alcaide, Q.~Anthony, et al.
\newblock RWKV: Reinventing RNNs for the Transformer era.
\newblock In \emph{Findings of the Association for Computational Linguistics: EMNLP}, pages 14048-14077, 2023.

\bibitem[Poli et al.(2023)]{poli2023hyena}
M.~Poli, S.~Massaroli, E.~Nguyen, et al.
\newblock Hyena hierarchy: Towards larger convolutional language models.
\newblock In \emph{International Conference on Machine Learning}, volume 202 of PMLR, pages 28043-28078, 2023.

\bibitem[Press et al.(2022)]{press2022alibi}
O.~Press, N.~A. Smith, and M.~Lewis.
\newblock Train short, test long: Attention with linear biases enables input length extrapolation.
\newblock In \emph{International Conference on Learning Representations}, 2022.

\bibitem[Rajpurkar et al.(2016)]{rajpurkar2016squad}
P.~Rajpurkar, J.~Zhang, K.~Lopyrev, and P.~Liang.
\newblock SQuAD: 100,000+ questions for machine comprehension of text.
\newblock In \emph{Proceedings of EMNLP}, pages 2383-2392, 2016.

\bibitem[Shaham et al.(2023)]{shaham2023zeroscrolls}
U.~Shaham, M.~Ivgi, A.~Efrat, J.~Berant, and O.~Levy.
\newblock ZeroSCROLLS: A zero-shot benchmark for long text understanding.
\newblock In \emph{Findings of the Association for Computational Linguistics: EMNLP}, pages 7977-7989, 2023.

\bibitem[Shi et al.(2023)]{shi2023distracted}
F.~Shi, X.~Chen, K.~Misra, N.~Scales, D.~Dohan, E.~H. Chi, N.~Sch\"arli, and D.~Zhou.
\newblock Large language models can be easily distracted by irrelevant context.
\newblock In \emph{International Conference on Machine Learning}, volume 202 of PMLR, pages 31210-31227, 2023.

\bibitem[Soboleva et al.(2023)]{soboleva2023slimpajama}
D.~Soboleva, F.~Al-Khateeb, R.~Myers, J.~R. Steeves, J.~Hestness, and N.~Dey.
\newblock SlimPajama: A 627B token cleaned and deduplicated version of RedPajama.
\newblock Cerebras dataset card and release, 2023.

\bibitem[Su et al.(2024)]{su2024roformer}
J.~Su, M.~Ahmed, Y.~Lu, S.~Pan, W.~Bo, and Y.~Liu.
\newblock RoFormer: Enhanced transformer with rotary position embedding.
\newblock \emph{Neurocomputing}, 568:127063, 2024.

\bibitem[Sun et al.(2024)]{sun2024massive}
M.~Sun, X.~Chen, J.~Kolter, and Z.~Liu.
\newblock Massive activations in large language models.
\newblock In \emph{Conference on Language Modeling}, 2024.

\bibitem[Vaswani et al.(2017)]{vaswani2017attention}
A.~Vaswani, N.~Shazeer, N.~Parmar, et al.
\newblock Attention is all you need.
\newblock In \emph{Advances in Neural Information Processing Systems}, 2017.

\bibitem[Waleffe et al.(2024)]{waleffe2024empirical}
R.~Waleffe, W.~Byeon, D.~Riach, et al.
\newblock An empirical study of Mamba-based language models.
\newblock \emph{arXiv:2406.07887}, 2024.

\bibitem[Xiao et al.(2024)]{xiao2024efficient}
G.~Xiao, Y.~Tian, B.~Chen, S.~Han, and M.~Lewis.
\newblock Efficient streaming language models with attention sinks.
\newblock In \emph{International Conference on Learning Representations}, 2024.

\bibitem[Xu et al.(2024)]{xu2024recomp}
F.~Xu, W.~Shi, and E.~Choi.
\newblock RECOMP: Improving retrieval-augmented LMs with compression and selective augmentation.
\newblock In \emph{International Conference on Learning Representations}, 2024.

\bibitem[Yang et al.(2024)]{yang2024qwen2}
A.~Yang, B.~Yang, B.~Zhang, et al.
\newblock Qwen2.5 technical report.
\newblock \emph{arXiv:2412.15115}, 2024.

\end{thebibliography}
}

\appendix

\section{Supplementary evidence and artifacts}
\label{app:artifacts}

This appendix maps every experimental phase to its executed evidence and includes the most informative committed plots that do not fit in the main paper.
All figures are generated from the cited JSON summaries by one deterministic plotting script.
The canonical figure files are SVG; the PDF companions embedded here are produced from the same plotting calls because standard \LaTeX{} engines do not include SVG directly.

\subsection{Phase and artifact ledger}

Table~\ref{tab:phase-ledger} separates executed measurements from their allowed interpretation.
The standard ten-position sweep contains ten prompts per question and uses the same question bundle at every position.

\begin{table}[t]
\caption{Executed evidence by phase.
Sample counts are questions unless otherwise stated.}
\label{tab:phase-ledger}
\centering
\scriptsize
\setlength{\tabcolsep}{5.5pt}
\begin{tabular}{@{}clll@{}}
\toprule
Phase & Executed comparison & Committed source & Interpretation in this paper \\
\midrule
1 & 200 QA; 50 key-value & \texttt{artifacts/phase1} & Calibration and positive control \\
2 & 3 models, $n=800$ each & \texttt{artifacts/phase2} & Primary family interaction \\
3 & pure and hybrid 8B, $n=800$ & \texttt{artifacts/phase3} & Matched but uncertain contrast \\
4 & 5 size pairs, $n=800$ each & \texttt{artifacts/phase4} & Descriptive scale trend \\
5 & 2 corpora, $n=800$ each & \texttt{artifacts/phase5} & Corpus scope check \\
6 & 3 models, 50 at two lengths & \texttt{artifacts/phase6} & Synthetic task scope check \\
7 & 100-200 per intervention; 1,200 states & \texttt{artifacts/phase7-mechanisms} & Hypothesis generation only \\
8 & 3 systems, $n=800$ each & \texttt{artifacts/phase8} & Descriptive prevalence check \\
\bottomrule
\end{tabular}
\end{table}

The dataset snapshot contains 2{,}655 rows and has SHA-256 checksum
\texttt{192a05b27af2b09eec33ca0c94bb5cf82bcaf70d78b3bdff1258df34bf37aab9}.
The fixed split allocates 800 rows to exploration and 1{,}855 to confirmation.
The confirmatory rows have not been evaluated.

\subsection{Calibration and the executed positive control}

Phase 1 calibrates the primary Pythia checkpoint before the cross-family comparison.
The no-gold floor is $0.095$, the gold-only ceiling is $0.650$, and moving the gold document from the middle to the first position changes accuracy by $+0.115$ (95\% CI $[0.055,0.175]$).
The key-value control produces a $+0.38$ edge-versus-middle effect, demonstrating that the position-intervention pipeline can recover an imposed positional signal.
This calibration figure is a single-position contrast (first slot minus middle slot) on the 200-question Phase 1 subset, and is not directly comparable to the primacy statistic reported in Table~\ref{tab:phase2}, which averages positions $\{0,1\}$ against $\{4,5\}$ on the 800-question exploratory split; the two floor/ceiling pairs and the two edge sizes accordingly differ.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/paper-phase1-calibration.pdf}
\caption{Phase 1 calibration.
The left panel shows key-value retrieval by slot.
The right panel shows the no-gold floor, middle-document condition, first-document condition, and gold-only ceiling for Pythia-2.8B.}
\label{fig:artifact-calibration}
\end{figure}

\subsection{Scale and corpus scope checks}

Figure~\ref{fig:artifact-scale-corpus} reports the complete Phase 4 and Phase 5 summaries.
The scale trend is inseparable from task capability at the smallest sizes.
The corpus comparison changes the accuracy level but does not detect an edge-shape difference under the fixed Mamba-2.8B architecture.

\begin{figure}[t]
\centering
\begin{minipage}[t]{0.62\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/paper-phase4-scale.pdf}
\textbf{(a) Approximate size pairs}
\end{minipage}\hfill
\begin{minipage}[t]{0.36\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/paper-phase5-corpus.pdf}
\textbf{(b) Fixed architecture, different corpora}
\end{minipage}
\caption{Additional scope checks.
Markers and colors retain the paper-wide encoding: attention-based Pythia models are blue stars, while state-space models are orange and use shape to distinguish checkpoint variants.}
\label{fig:artifact-scale-corpus}
\end{figure}

\subsection{Synthetic retrieval}

The Phase 6 forest plot in Figure~\ref{fig:artifact-task} separates the QA estimates from 2K-token needle retrieval.
Open markers denote the synthetic task and filled markers denote QA.
Mamba and Mamba-2 saturate the needle task, so the absence of an edge effect for those models is a ceiling result rather than evidence of better middle-context behavior.
Only the summary and plots for this phase are committed; raw generations and an environment manifest are not available in the repository snapshot.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/paper-phase6-task.pdf}
\caption{Primacy and recency across the main QA task and 2K-token RULER needle retrieval.
Horizontal intervals are 95\% question-bootstrap confidence intervals.}
\label{fig:artifact-task}
\end{figure}

\subsection{Mechanism-oriented measurements and prompt interventions}

Figure~\ref{fig:artifact-mechanisms} combines the valid Phase 7 measurements.
The sink statistic and probe results are associations, not interventions.
The attempted hybrid attention-sink block is excluded because the model's custom attention path did not honor the hook, so the intervention did not change the computation.
The prompt variants jointly alter query position, repetition, and token layout and therefore do not isolate one mechanism.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/paper-phase7-mechanisms.pdf}
\caption{Phase 7 mechanism-oriented evidence.
The sink panel compares final-layer initial-token attention share with primacy.
The probe panel includes shuffled-label controls.
The prompt panel reports edge effects for the executed query and template variants.}
\label{fig:artifact-mechanisms}
\end{figure}

\subsection{Additional 7B-8B systems}

Figure~\ref{fig:artifact-production} shows the full position curves for the descriptive system comparison.
Marker shape and line style distinguish systems without adding colors.
These checkpoints differ on several factors beyond the sequence mixer, so the figure is evidence of prevalence rather than a controlled architecture result.

\begin{figure}[t]
\centering
\includegraphics[width=0.72\linewidth]{figures/paper-phase8-production.pdf}
\caption{Position curves for Nemotron-H-8B, Llama-3.1-8B, and Qwen2.5-7B on the 800-question exploratory split.}
\label{fig:artifact-production}
\end{figure}

\section{Integrity and provenance notes}
\label{app:integrity}

The primary standard sweep yields 9{,}600 generations per model: 8{,}000 position prompts, 800 no-gold prompts, and 800 gold-only prompts.
The paired bootstrap resamples complete question bundles and preserves prompt and distractor identity across positions.
No generation failure or scored null is reported in the primary summaries.
Prompt-length checks keep the compared position prompts within one token.

The Phase 3 result was independently re-run under a clean source tree at commit \texttt{33d6bb5}, reproducing the original summary exactly; see \texttt{artifacts/phase3/rerun-33d6bb5/REPORT.md}.
A 50-generation blinded audit sample exists, but the human labels were not completed.
The committed negative and distractor-order controls passed on the exploratory Pythia sample only and do not test the 8B Megatron checkpoints.

\begin{table}[t]
\caption{Execution environments recorded by the phase artifacts.}
\label{tab:compute}
\centering
\small
\begin{tabular}{@{}lll@{}}
\toprule
Phases & Recorded accelerator & Scope \\
\midrule
1 and 5 & NVIDIA A10G & Calibration and corpus check \\
2-4 & NVIDIA A40 & Primary, matched, and scale comparisons \\
6 & NVIDIA T4 & Synthetic retrieval \\
7 & NVIDIA L40S and T4 & Prompt, sink, and probe studies \\
8 & NVIDIA L40S & Additional 7B-8B systems \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Checkpoint provenance}

Table~\ref{tab:checkpoints} records the immutable revisions for the checkpoints that define the primary, matched, corpus, and additional-system comparisons.
The complete scale-suite registry appears in \texttt{src/mixing\_matters/models.py}.

\begin{table}[t]
\caption{Selected checkpoint repositories and pinned revisions.}
\label{tab:checkpoints}
\centering
\scriptsize
\begin{tabular}{@{}p{0.22\linewidth}p{0.46\linewidth}p{0.22\linewidth}@{}}
\toprule
Paper key & Repository identifier & Revision \\
\midrule
Pythia-2.8B & \texttt{EleutherAI/pythia-2.8b} & \texttt{2a259cdd96a4} \\
Mamba-2.8B & \texttt{state-spaces/mamba-2.8b-hf} & \texttt{96c48e0292b6} \\
Mamba-2 2.7B & \texttt{AntonV/mamba2-2.7b-hf} & \texttt{ef542707386f} \\
Pure Mamba-2 8B & \texttt{nvidia/mamba2-8b-3t-4k} & \texttt{b915550c63ba} \\
Hybrid Mamba-2 8B & \texttt{nvidia/mamba2-hybrid-8b-3t-4k} & \texttt{35e8852e2240} \\
SlimPajama Mamba-2.8B & \texttt{state-spaces/mamba-2.8b-slimpj} & \texttt{a7bdd41af90c} \\
Nemotron-H-8B & \texttt{nvidia/Nemotron-H-8B-Base-8K} & \texttt{94ea861e008c} \\
Llama-3.1-8B & \texttt{meta-llama/Llama-3.1-8B} & \texttt{d04e592bb4f6} \\
Qwen2.5-7B & \texttt{Qwen/Qwen2.5-7B} & \texttt{d14972939875} \\
\bottomrule
\end{tabular}
\end{table}

Elapsed time and aggregate compute were not logged consistently enough to report a defensible total.
Checkpoint repository identifiers and immutable revisions are recorded in \texttt{src/mixing\_matters/models.py} and the per-run manifests.
Some evaluated converted checkpoint mirrors do not declare complete license metadata in their model cards, so this snapshot does not claim a complete asset-license audit.

\newpage
\input{checklist.tex}

\end{document}
